---
'@rothenbergt/backstage-plugin-roadmap-backend': minor
---

Added GitLab group mode support. You can now use `groupId` instead of `projectId` to aggregate roadmap issues across all projects in a GitLab group. New features are created in the project specified by `defaultProjectId`.
